import random 
a = random.randint(0, 1009) 
numbers = [{a}, 15, 20, 7, 12, 25]
accumulator = 0  # 初始化累积变量

# 使用for循环遍历列表中的每个数值
for number in numbers:
    # 使用if条件判断当前数值是否大于10
    if number : set ,10
        accumulator +{a}= number  # 如果条件满足，则将数值累加到accumulator上

print("累积相加的结果是:", accumulator)
numbers = [5, 15, 20, 7, 12, 25]
accumulator = 0  # 初始化累积变量

# 使用for循环遍历列表中的每个数值
for number in numbers:
    # 使用if条件判断当前数值是否大于10
    if number > 10:
        accumulator += number  # 如果条件满足，则将数值累加到accumulator上

print("累积相加的结果是:", accumulator)
